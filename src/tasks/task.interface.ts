export enum TaskStatus {
    OPEN = "OPEN",
    IN_PROCESS = "IN_PROCESS",
    DONE = "DONE"
}

/*
* Since we already have the entity, so we can remove the temp Task interface
* */
// export interface Task {
//     id: string;
//     title: string,
//     content: string;
//     status: TaskStatus;
// }

